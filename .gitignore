# 本地 / IDE 数据（不属于游戏本体）
.codebuddy/
node_modules/

# 临时脚本、提交信息、日志（统一以 _ 开头，避免误提交）
_*
*.log

# 网盘 / 同步客户端的临时文件（百度网盘会在被同步的文件旁生成 .cfg，
# 文件变动时的「写入被回滚」现象就来自它 —— 不要提交）
*.baiduyun.uploading.cfg
*~$*
.DS_Store
Thumbs.db

# 开发期的临时产物：浏览器自动化快照、出图脚本与中间图
.playwright-cli/
_*.py
_*.mjs
_smoke.mjs
*.png
!assets/*.png
